apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mod-password-validator.fullname" . }}-test-connection"
  labels:
    {{- include "mod-password-validator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mod-password-validator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
