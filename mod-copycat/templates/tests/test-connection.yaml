apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mod-copycat.fullname" . }}-test-connection"
  labels:
    {{- include "mod-copycat.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mod-copycat.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
