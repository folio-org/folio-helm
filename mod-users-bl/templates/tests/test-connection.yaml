apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mod-users-bl.fullname" . }}-test-connection"
  labels:
    {{- include "mod-users-bl.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mod-users-bl.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
