apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mod-notes.fullname" . }}-test-connection"
  labels:
    {{- include "mod-notes.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mod-notes.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
