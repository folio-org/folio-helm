apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mod-permissions.fullname" . }}-test-connection"
  labels:
    {{- include "mod-permissions.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mod-permissions.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
