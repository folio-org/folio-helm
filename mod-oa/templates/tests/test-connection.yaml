apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mod-oa.fullname" . }}-test-connection"
  labels:
    {{- include "mod-oa.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mod-oa.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
