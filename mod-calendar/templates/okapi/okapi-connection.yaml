apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mod-calendar.fullname" . }}-okapi-connection"
  labels:
    {{- include "mod-calendar.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mod-calendar.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
