apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mod-inventory.fullname" . }}-test-connection"
  labels:
    {{- include "mod-inventory.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mod-inventory.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
