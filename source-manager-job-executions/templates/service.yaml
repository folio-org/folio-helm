apiVersion: v1
kind: Service
metadata:
  name: {{ include "source-manager-job-executions.fullname" . }}
  labels:
    {{- include "source-manager-job-executions.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "source-manager-job-executions.selectorLabels" . | nindent 4 }}
