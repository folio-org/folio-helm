apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mod-tags.fullname" . }}-okapi-registration"
  labels:
    {{- include "mod-tags.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['okapi:9130']
  restartPolicy: Never
