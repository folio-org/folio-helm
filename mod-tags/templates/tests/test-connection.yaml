apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mod-tags.fullname" . }}-test-connection"
  labels:
    {{- include "mod-tags.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mod-tags.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
