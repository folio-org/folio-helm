apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mod-template-engine.fullname" . }}-test-connection"
  labels:
    {{- include "mod-template-engine.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mod-template-engine.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
