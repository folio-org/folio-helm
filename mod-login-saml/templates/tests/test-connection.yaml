apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mod-login-saml.fullname" . }}-test-connection"
  labels:
    {{- include "mod-login-saml.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mod-login-saml.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
