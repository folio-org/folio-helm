apiVersion: v1
kind: Service
metadata:
  name: {{ include "mod-inventory-pronto.fullname" . }}
  labels:
    {{- include "mod-inventory-pronto.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.containerPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "mod-inventory-pronto.selectorLabels" . | nindent 4 }}
