apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mod-codex-mux.fullname" . }}-test-connection"
  labels:
    {{- include "mod-codex-mux.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mod-codex-mux.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
