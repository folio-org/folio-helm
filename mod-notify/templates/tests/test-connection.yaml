apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mod-notify.fullname" . }}-test-connection"
  labels:
    {{- include "mod-notify.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mod-notify.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
