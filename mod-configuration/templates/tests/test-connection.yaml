apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mod-configuration.fullname" . }}-test-connection"
  labels:
    {{- include "mod-configuration.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mod-configuration.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
