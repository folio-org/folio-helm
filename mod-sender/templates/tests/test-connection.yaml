apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mod-sender.fullname" . }}-test-connection"
  labels:
    {{- include "mod-sender.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mod-sender.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
