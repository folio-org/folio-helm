apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mod-codex-inventory.fullname" . }}-test-connection"
  labels:
    {{- include "mod-codex-inventory.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mod-codex-inventory.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
