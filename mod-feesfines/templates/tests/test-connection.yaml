apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mod-feesfines.fullname" . }}-test-connection"
  labels:
    {{- include "mod-feesfines.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mod-feesfines.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
