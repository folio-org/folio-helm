apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mod-event-config.fullname" . }}-test-connection"
  labels:
    {{- include "mod-event-config.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mod-event-config.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
